<div class='contents row'>
  <div class='content_post'>
    <% if user_signed_in? && current_user.id==@post.user_id %>
      <div class='more'>
        <ul class='more_list'>
          <li><%= link_to '編集',"..",method: :get %></li>
          <li><%= link_to '削除', "/areas/#{@post.id}/small_areas/",method: :delete %></li>
        </ul>
      </div>
    <% end %>
  <%= simple_format(@post.text) %>
  </div>
</div>
